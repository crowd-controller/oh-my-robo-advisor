"""레코드형 YAML 로더. 계층 병합·env 오버라이드 대상이 아니다.

`config.yaml` 계층과 물리적으로 다른 로더를 쓰는 이유는 병합 규칙이 다르기 때문이다 —
매핑은 재귀 병합이지만 레코드 파일은 통째로 리스트라 오버레이에 한 항목만 적으면
전체가 사라진다. 그 사고 경로 자체를 없앤다.

정본: 설계 04 §5 [DD-04-1]
"""
