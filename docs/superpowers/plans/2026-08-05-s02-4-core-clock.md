# S02-4 Core Clock Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 설계 02 §8의 단일 시간축 계약을 구현해 라이브 코드는 실제 대기하고 시뮬레이션은 벽시계 대기 없이 같은 API로 진행되게 한다.

**Architecture:** `core.clock`이 `Clock` ABC, `SystemClock`, `SimClock`, `KST`의 유일한 정의 위치다. 모든 입력 시각은 aware 여부를 검사하고 시뮬레이션 내부 시각은 UTC로 정규화하며, `money`는 같은 `KST` 상수를 재사용한다.

**Tech Stack:** Python 3.12 표준 라이브러리(`abc`, `asyncio`, `datetime`, `zoneinfo`), pytest, pytest-asyncio, ruff, mypy, import-linter.

## Global Constraints

- 계획 문서는 설계 문서보다 상위 정본이며, Clock 정의의 소유 문서는 `docs/design/02-domain-model.md`다.
- 구현 범위는 `docs/design/17-implementation-progress.md:171`의 S02-4 하나다.
- 공개 API는 `Clock.now_utc`, `Clock.now_kst`, `Clock.sleep_until`, `Clock.sleep_for`, `SystemClock`, `SimClock`, `KST`다.
- `now_utc()`는 UTC aware `datetime`을 반환한다.
- naive `datetime`과 시뮬레이션 시각 후퇴는 `InvariantViolation`으로 거부한다.
- 과거 또는 현재의 `sleep_until`은 즉시 반환하고 시뮬레이션 대기는 벽시계 시간을 소비하지 않는다.
- 전역 Clock 싱글턴과 새 의존성을 추가하지 않는다.
- `datetime.now`와 `asyncio.sleep` 직접 사용은 `core.clock` 안에서만 한다.
- 전역 호출 금지 AST 검사는 `docs/design/17-implementation-progress.md:184`의 S03-7 소유이므로 이번 단위에 선점 구현하지 않는다.
- 새 production 코드는 새 테스트가 누락 모듈 때문에 실패한 것을 확인한 뒤 작성한다.

---

### Task 1: Implement S02-4 `core.clock`

**Files:**
- Create: `src/omra/core/clock.py`
- Modify: `src/omra/core/money.py`
- Create: `tests/unit/core/test_clock.py`
- Modify: `docs/design/17-implementation-progress.md`

**Interfaces:**
- Consumes: `omra.core.errors.InvariantViolation`; Python `datetime.UTC`; `asyncio.sleep`.
- Produces: `KST: Final[ZoneInfo]`, `Clock`, `SystemClock`, `SimClock`.
- Preserves: `omra.core.money.KST` as a re-export of the canonical `core.clock.KST` object.

- [x] **Step 1: Write failing Clock behavior tests**

Create `tests/unit/core/test_clock.py` with real calls covering these independent breaks:

```python
from __future__ import annotations

from datetime import UTC, datetime, timedelta
from time import perf_counter

import pytest

from omra.core import clock as clock_module
from omra.core.clock import KST, Clock, SimClock, SystemClock
from omra.core.errors import InvariantViolation
from omra.core.money import KST as MONEY_KST


START = datetime(2026, 8, 5, tzinfo=UTC)


def test_clock_abstract_surface_is_exact() -> None:
    assert Clock.__abstractmethods__ == frozenset({"now_utc", "sleep_until"})


def test_clock_module_public_exports_are_exact() -> None:
    assert clock_module.__all__ == ["Clock", "KST", "SimClock", "SystemClock"]


def test_money_reexports_the_canonical_kst() -> None:
    assert MONEY_KST is KST


def test_sim_clock_normalizes_aware_start_to_utc() -> None:
    clock = SimClock(datetime(2026, 8, 5, 9, 0, tzinfo=KST))
    assert clock.now_utc() == datetime(2026, 8, 5, 0, 0, tzinfo=UTC)
    assert clock.now_utc().tzinfo is UTC


def test_sim_clock_rejects_naive_start() -> None:
    with pytest.raises(InvariantViolation):
        SimClock(datetime(2026, 8, 5))


def test_sim_clock_rejects_naive_set_target() -> None:
    clock = SimClock(START)
    with pytest.raises(InvariantViolation):
        clock.set_to(datetime(2026, 8, 6))


@pytest.mark.asyncio
async def test_sim_clock_rejects_naive_sleep_target() -> None:
    clock = SimClock(START)
    with pytest.raises(InvariantViolation):
        await clock.sleep_until(datetime(2026, 8, 6))


def test_sim_clock_rejects_backward_set() -> None:
    clock = SimClock(START)
    with pytest.raises(InvariantViolation):
        clock.set_to(START - timedelta(microseconds=1))


def test_sim_clock_rejects_negative_advance() -> None:
    clock = SimClock(START)
    with pytest.raises(InvariantViolation):
        clock.advance(timedelta(microseconds=-1))


def test_sim_clock_advance_accumulates_exactly() -> None:
    clock = SimClock(START)
    clock.advance(timedelta(minutes=30))
    clock.advance(timedelta(seconds=45))
    assert clock.now_utc() == datetime(2026, 8, 5, 0, 30, 45, tzinfo=UTC)


@pytest.mark.parametrize(
    "target",
    [START, START - timedelta(days=1)],
)
@pytest.mark.asyncio
async def test_sim_clock_nonfuture_sleep_does_not_rewind(target: datetime) -> None:
    clock = SimClock(START)
    await clock.sleep_until(target)
    assert clock.now_utc() == START


@pytest.mark.asyncio
async def test_sim_clock_future_sleep_advances_without_wall_wait() -> None:
    clock = SimClock(START)
    target = datetime(2036, 8, 5, tzinfo=UTC)
    wall_start = perf_counter()
    await clock.sleep_until(target)
    assert perf_counter() - wall_start < 0.1
    assert clock.now_utc() == target


@pytest.mark.asyncio
async def test_sleep_for_uses_the_same_clock_timeline() -> None:
    clock = SimClock(START)
    await clock.sleep_for(timedelta(hours=2, seconds=3))
    assert clock.now_utc() == datetime(2026, 8, 5, 2, 0, 3, tzinfo=UTC)


def test_system_clock_now_utc_is_current_aware_utc() -> None:
    clock = SystemClock()
    before = datetime.now(UTC)
    observed = clock.now_utc()
    after = datetime.now(UTC)
    assert before <= observed <= after
    assert observed.tzinfo is UTC


def test_system_clock_now_kst_has_fixed_nine_hour_offset() -> None:
    observed = SystemClock().now_kst()
    assert observed.tzinfo is KST
    assert observed.utcoffset() == timedelta(hours=9)


@pytest.mark.asyncio
async def test_system_clock_rejects_naive_sleep_target() -> None:
    with pytest.raises(InvariantViolation):
        await SystemClock().sleep_until(datetime(2026, 8, 5))


@pytest.mark.asyncio
async def test_system_clock_really_waits_for_future_deadline() -> None:
    clock = SystemClock()
    deadline = clock.now_utc() + timedelta(milliseconds=30)
    await clock.sleep_until(deadline)
    assert clock.now_utc() >= deadline


@pytest.mark.asyncio
async def test_system_clock_past_sleep_returns_immediately() -> None:
    clock = SystemClock()
    wall_start = perf_counter()
    await clock.sleep_until(clock.now_utc() - timedelta(days=1))
    assert perf_counter() - wall_start < 0.1
```

- [x] **Step 2: Run the new test and verify RED**

Run:

```bash
.venv/bin/python -m pytest tests/unit/core/test_clock.py -q
```

Expected: collection fails with `ModuleNotFoundError: No module named 'omra.core.clock'`. Syntax, fixture, or marker failures are not acceptable RED evidence.

Observed: exit 2 with package-level `ImportError: cannot import name 'clock' from 'omra.core'`; the independent review confirmed this was the same missing-module boundary and not a syntax, fixture, or marker failure.

- [x] **Step 3: Implement the minimum production API**

Create `src/omra/core/clock.py` around these mechanics:

```python
from __future__ import annotations

import asyncio
from abc import ABC, abstractmethod
from datetime import UTC, datetime, timedelta
from typing import Final
from zoneinfo import ZoneInfo

from omra.core.errors import InvariantViolation

__all__ = ["Clock", "KST", "SimClock", "SystemClock"]

KST: Final = ZoneInfo("Asia/Seoul")


def _require_aware(value: datetime) -> datetime:
    if value.tzinfo is None or value.utcoffset() is None:
        raise InvariantViolation("naive datetime 은 Clock 시간축에 들어올 수 없다")
    return value


class Clock(ABC):
    @abstractmethod
    def now_utc(self) -> datetime:
        raise NotImplementedError

    def now_kst(self) -> datetime:
        return _require_aware(self.now_utc()).astimezone(KST)

    @abstractmethod
    async def sleep_until(self, t: datetime) -> None:
        raise NotImplementedError

    async def sleep_for(self, delta: timedelta) -> None:
        await self.sleep_until(self.now_utc() + delta)


class SystemClock(Clock):
    def now_utc(self) -> datetime:
        return datetime.now(UTC)

    async def sleep_until(self, t: datetime) -> None:
        target = _require_aware(t).astimezone(UTC)
        delay = (target - self.now_utc()).total_seconds()
        if delay > 0:
            await asyncio.sleep(delay)


class SimClock(Clock):
    def __init__(self, start: datetime) -> None:
        self._current = _require_aware(start).astimezone(UTC)

    def now_utc(self) -> datetime:
        return self._current

    def set_to(self, t: datetime) -> None:
        target = _require_aware(t).astimezone(UTC)
        if target < self._current:
            raise InvariantViolation("SimClock 시각은 뒤로 이동할 수 없다")
        self._current = target

    def advance(self, delta: timedelta) -> None:
        self.set_to(self._current + delta)

    async def sleep_until(self, t: datetime) -> None:
        target = _require_aware(t).astimezone(UTC)
        if target > self._current:
            self.set_to(target)
```

Modify `core.money` to import `KST` from `core.clock` and remove its duplicate `ZoneInfo("Asia/Seoul")` definition while retaining `KST` in `money.__all__` for compatibility.

- [x] **Step 4: Run focused tests and verify GREEN**

Run:

```bash
.venv/bin/python -m pytest tests/unit/core/test_clock.py tests/unit/core/test_money.py -q
```

Expected: every Clock and money test passes without warnings.

Observed: `89 passed`.

- [x] **Step 5: Run repository gates**

Run the available project-local executables:

```bash
.venv/bin/ruff check .
.venv/bin/ruff format --check .
.venv/bin/mypy
.venv/bin/lint-imports
.venv/bin/python -m pytest -m "unit or property or arch"
```

Expected: every command exits 0. Record the exact final pytest count before updating the progress document.

Observed: ruff check·format, mypy, 19 import contracts all exited 0; `368 passed`.

- [x] **Step 6: Review, update progress, commit, and push**

After independent code review has no unresolved Critical/Important finding, update `docs/design/17-implementation-progress.md` in the same change:

```text
M0 completed: 11 / 54 (20%)
Total completed: 11 / 307 (3.6%)
Current: S02-5
Previous: S02-4
Next: S03-1
S02-4 status: ☑
S02-4 commit title: feat(core): 결정론적 Clock 시간축 구현
```

Append the fresh test count to the S02-4 DoD only after reading the full gate output. Stage only the S02-4 code, tests, plan, and progress document. Commit with a detailed Korean body containing the implementation unit and design basis, ending with exactly:

```text
Co-Authored-By: Codex
```

Push the resulting `main` commit to its configured upstream and verify local `HEAD` equals `origin/main`.
